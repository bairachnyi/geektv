# PlatformIO
.pio/
.platformio/
.venv/
.pioenvs/
.piolibdeps/
.vscode/
*.bin
*.elf
*.map

# pioarduino / ESP-IDF build artifacts (generated by the ESP32 builds)
.dummy/
managed_components/
sdkconfig
sdkconfig.*
/CMakeLists.txt

# Build output we DO want to keep out of git (CI produces it)
firmware*.bin

# Reference material (proprietary / third-party — not redistributable)
sources/

# Python (daemon / tools)
__pycache__/
*.pyc

# Generated by daemon/install.bat
daemon/run_daemon.vbs

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

.playwright-mcp/

# pioarduino-generated dependency lock
dependencies.lock

# manual quotes-branch staging dir (see .github/workflows/quotes.yml)
/out/

# Local GitHub bridge credentials (contains access tokens)
/emulator/config.local.json
/emulator/device-config.local.json
/emulator/github-events.local.json

# Read-only snapshots from the physical stock device. These may contain
# user-owned filenames or sanitized local state and must stay on this Mac.
/research/stock-v9.0.51/raw/
